# models/

One directory per model package: pinned URL and sha256 per artifact, roles, the capability record. See [docs/PACKAGES.md](https://github.com/getmaipai/.github/blob/main/docs/PACKAGES.md)
in `.github` for the manifest format. Empty until the first package lands;
see `../docs/dev.md` for the step-0 status.
