# schema/

Mirrored from `home/spec/`, not hand-edited here. Holds the JSON Schema
shapes every package's manifest and recipe conform to, so catalog CI can
run the conformance suite without depending on a private checkout of
`home`. Empty until `home/spec/` exists to mirror from.
