# tools/

The catalog's own tooling: lint, pack, sign, index, scorecard, and the
`check` CLI a contributor runs before proposing a package. Not built yet;
see `../docs/dev.md` for the step-0 status. Until this exists, no package
can be verified or published from this repo.
